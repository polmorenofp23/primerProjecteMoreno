Esta carpeta es solo estática (no PHP):

Aquí:

index.html construye el layout del panel:

sidebar, tablas, formularios, etc.

Los ficheros JS hacen:

fetch() a tus APIs PHP para obtener pedidos, productos, logs, etc.

manipulan el DOM (crear filas de tabla, ordenar, filtrar…)

usan localStorage/sessionStorage como te piden.